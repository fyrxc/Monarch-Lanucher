<UserControl x:Class="MonarchLauncher.App.Views.Pages.ModsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="UserControl_Loaded">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Mods" Style="{StaticResource Text.PageTitle}" VerticalAlignment="Center" />
                <TextBlock Text="{Binding ResultCountText}" Style="{StaticResource Text.Metadata}" VerticalAlignment="Center" Margin="10,3,0,0" />
                <TextBlock Text="•" Foreground="{DynamicResource Brush.TextMuted}" Margin="9,2,9,0" />
                <TextBlock Text="{Binding StatusText}" Style="{StaticResource Text.Metadata}" VerticalAlignment="Center" Margin="0,3,0,0" />
            </StackPanel>

            <Button Grid.Column="1"
                    Content="Refresh"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource Style.SecondaryButton}"
                    MinWidth="82" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"
                     Style="{StaticResource Style.SearchBox}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Search installed mods by name, Workshop ID, or path" />
            <TextBlock Grid.Column="2"
                       Text="Steam Workshop • DayZ 221100"
                       Style="{StaticResource Text.Metadata}"
                       VerticalAlignment="Center" />
        </Grid>

        <Border Grid.Row="4"
                Background="{DynamicResource Brush.Panel}"
                BorderBrush="{DynamicResource Brush.Border}"
                BorderThickness="1"
                CornerRadius="6"
                ClipToBounds="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="34" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#1C2229"
                        BorderBrush="{DynamicResource Brush.Border}"
                        BorderThickness="0,0,0,1"
                        Padding="10,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2.4*" />
                            <ColumnDefinition Width="1.1*" />
                            <ColumnDefinition Width="3.2*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="MOD" Style="{StaticResource Text.SectionLabel}" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="WORKSHOP ID" Style="{StaticResource Text.SectionLabel}" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="2" Text="INSTALLED PATH" Style="{StaticResource Text.SectionLabel}" VerticalAlignment="Center" />
                    </Grid>
                </Border>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding FilteredMods}"
                         Style="{StaticResource Style.ServerList}"
                         ItemContainerStyle="{StaticResource Style.ServerListItem}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid ToolTip="{Binding Path}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2.4*" />
                                    <ColumnDefinition Width="1.1*" />
                                    <ColumnDefinition Width="3.2*" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="▦" Foreground="{DynamicResource Brush.TextSecondary}" Margin="0,0,9,0" VerticalAlignment="Center" />
                                    <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                                </StackPanel>
                                <TextBlock Grid.Column="1" Text="{Binding WorkshopId}" Style="{StaticResource Text.Metadata}" VerticalAlignment="Center" />
                                <TextBlock Grid.Column="2" Text="{Binding Path}" Style="{StaticResource Text.Metadata}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
